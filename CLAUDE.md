# petcare — 프로젝트 작업 규칙

> 전역 규칙(`~/.claude/CLAUDE.md`)을 따르되, **충돌하면 이 파일이 이긴다.**
> 이 파일은 매 세션 로드된다. 짧게 유지한다. 코드에서 읽으면 아는 것은 쓰지 않는다.

---

## 1. 지금 어디까지 왔나

- **Phase 0 완료** — `prototype/index.html` (localStorage `petcare.proto.v3`, 22화면, 3역할)
- **Phase 1 기술 항목 완료** (2026-08-12) — Supabase 이식. `app/`(Vite) → https://doongji-kappa.vercel.app
  M1~M7 + P0 기능 20개 이식, RLS 침투 3케이스 확인. **남은 완료 조건은 현장 검증**
  (실제 알바 1건 투입 · 보호자 인터뷰 1명) — `docs/11-PRD-v3.md` §9
- 정본 순서: **`docs/11-PRD-v3.md` > `docs/09-방향전환.md` > 나머지**
  (`07-PRD`, `02~06`은 폐기·이력 보관용. 충돌 시 위가 이긴다)
- 유일한 1차 근거: `docs/08-사용자-인터뷰.md` — **여기와 충돌하는 추론은 버린다**

---

## 2. 이 서비스의 축 (설계 판단의 기준)

> **감시가 아니라 회계.** 시터에게 판단을 요구하면 완화된다 → 판단할 게 없는 것(사진·시각)만
> 요구하고, 판정은 보호자가 한다.

기능을 더하거나 바꿀 때 아래를 먼저 확인한다. 어기면 **하기 전에 말한다.**

1. **비율(%)은 본인이 통제하는 것에만 쓴다**
2. **"확인 안 해도 불이익 없음"은 문구와 집계가 동시에 지켜야 한다**
3. **확인된 것과 본인이 쓴 것을 섞지 않는다** (✅확인됨 / ⓘ본인이 씀 / 앨범 / 종료 후 기록)
4. **느슨함이 기본, 엄격함이 예외** (지연 벌점 없음, 사진 강제 없음 — "숙제처럼 느껴지면" 실패)
5. **되돌리기 기준은 시간이 아니라 상대가 반응했는가** (소명 전까지만 철회 가능)

---

## 3. 작업 방식

### 3-1. 사용자가 결정한다
- UI·문구·플로우는 **사용자 결정이 최우선**. 내 판단과 다르면 **근거를 대고 한 번 말한 뒤 따른다**
- 설계상 문제가 보이면 구현 전에 말한다. 구현 후 "사실 이게 문제였다"는 늦다
- 스크린샷 마킹·요소 선택으로 오는 지시는 그 자리를 정확히 고친다. 주변까지 임의로 손대지 않는다

### 3-2. 패치 규율 (사고 2회 발생 — 반드시 지킬 것)
- 파일 수정은 python heredoc + **replace마다 `assert`**, 적용 목록을 출력한다
- **배치 중 하나라도 assert 실패하면 그 배치는 통째로 미적용이다.** 이어서 적용할 때
  남은 replace 전체를 재확인한다 — 앞 조각이 빠진 채 뒤 조각만 넣으면 ReferenceError로 빈 화면이 된다
- 수정 후 항상 `node --check` (인라인 스크립트 추출해서)

### 3-3. 검증 (자기평가 금지 — 화면으로 증명한다)
- 수정 뒤 **브라우저에서 실제로 눌러본다.** "될 것이다"는 보고하지 않는다
- `file://`는 캐시가 강하다 → **`?v=N`을 매번 올린다**
- 화면 문구 검사는 `<script>` 제거 후 `textContent`로 (안 하면 소스가 잡혀 오탐)
- 상태는 localStorage 주입이 아니라 **UI를 실제로 주행해서** 만든다 (주입은 리로드에서 유실됨)
- 빈 화면이 뜨면 `document.title` 확인 — `window.onerror` 훅이 에러를 title에 넣는다
- 검증 결과는 항목별 true/false 배열로 출력하고, **false가 있으면 고치기 전엔 완료라고 하지 않는다**

### 3-4. 문서 갱신 (작업과 동시에)
- 의미 있는 변경은 `docs/09-방향전환.md`에 **`# N차 수정`** 추가:
  사용자 정의(인용) → 무엇을 왜 → **검증** → **「이 수정이 틀렸다면」**(기각 조건)
- 잔손질은 `## N차 보강`으로 묶어도 된다. 단 **버그를 고쳤으면 원인과 재발 방지를 남긴다**
- 스코프·정책·데이터 모델이 바뀌면 `docs/11-PRD-v3.md`도 같이 고친다 (PRD가 정본)
- 사실에는 근거를, 추론에는 `[추측]`을 붙인다. 수치·API·버전은 확인하고 쓴다

---

## 4. 코드 규약 (prototype)

- 단일 파일 vanilla JS. 프레임워크·빌드 도입 금지
- 화면은 `{ title, back, right, rightExtra, overlay, fab, body[], foot[], hint }`를 반환하는 함수.
  `SCREENS[role][screen]`에 등록한다
- `el(tag, attrs, kids)` 헬퍼 사용. **자식 배열에 null이 섞이면 `filter(Boolean)`** (nav/body/foot 모두 적용됨)
- 상태는 `S` 하나 + `save()`/`load()`. **`load()`는 필드별 병합 + 배열 가드 + 마이그레이션**을 유지한다
  (얕은 병합으로 새 필드가 사라진 사고 있었음)
- 새 상태 필드를 추가하면 `SEED`와 `load()` 가드를 **동시에** 추가한다
- CSS 클래스명 중복 금지 — `.bar`가 두 곳에 정의돼 역할 바가 붕괴한 적 있다

---

## 5. Phase 1 (Supabase) 규칙

- **기능 추가 금지.** 지금 동작하는 것을 그대로 옮기는 작업이다
- 아래 4개는 **서버가 강제**한다 (클라이언트 신뢰 금지):
  1. 재제출 → 판정·열람 무효화, **이의는 보존**
  2. 시터 소명 후 보호자 철회 불가
  3. 일기 후기 = (아이, 계약)당 1편
  4. 전 항목 기록 후에만 `finished_at`
- 보호자는 **무계정**(초대 토큰), 공개는 **동의한 계약의 읽기 전용**. RLS로 막고, 우회 시도로 검증한다
- 사진은 Storage로. localStorage 의존 코드는 남기지 않는다
- 스키마는 `docs/11-PRD-v3.md` §6-2에서 시작한다. 바꾸면 PRD를 고친다

---

## 6. 하지 않는 것

- 요청하지 않은 기능·카피·섹션 추가
- 사용자가 정한 문구를 "더 자연스럽게" 임의 수정
- 검증 없이 "완료" 보고. 막히면 **막혔다고, 무엇이 막았는지** 쓴다
- 실시간 영상·GPS·랭킹·지연 벌점 (`docs/11-PRD-v3.md` §3-4 Non-goals)
- 문서를 새로 만들어 정본을 분산시키기 — 기존 파일에 이어 쓴다
